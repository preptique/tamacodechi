{
  "name": "tamacodechi",
  "version": "0.2.0",
  "description": "MCP Tamagotchi coding buddy companion — summoned ASCII pet that lives in your CLI",
  "type": "module",
  "main": "tamacodechi.js",
  "bin": {
    "tamacodechi": "./install.js"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test dist/*.test.js",
    "install": "node install.js",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
